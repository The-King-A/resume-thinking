package com.resumethinking.platform.profiles;

public enum LlmProfileDiagnosticCode {
    INVALID_API_KEY,
    PROVIDER_FORBIDDEN,
    PROVIDER_ENDPOINT_NOT_FOUND,
    PROVIDER_RATE_LIMITED,
    PROVIDER_UNAVAILABLE,
    PROVIDER_RESPONSE_INVALID
}
