# ai-resume-thinking 新电脑运行准备指南

本文用于在另一台 Windows 电脑上克隆并运行 ai-resume-thinking 简历驱动岗位匹配与模拟面试平台。

## 一、项目组成

项目由三个独立服务组成：

- `front`：Vue 前端页面，默认端口 `5173`
- `back/java`：Spring Boot 公共 API，默认端口 `8080`
- `back/python`：FastAPI 简历分析服务，默认端口 `8000`

另外还需要：

- MySQL：保存用户、简历、匹配任务和分析结果
- Redis：提供缓存和任务运行所需的本地依赖

## 二、安装运行环境

新电脑需要安装：

- Git
- JDK 21
- Python 3.11
- Node.js 和 pnpm
- MySQL 8.x
- Docker Desktop

本项目要求 Python 版本满足 `>=3.11,<3.12`，不要使用 Python 3.13。

Java 项目已经包含 Maven Wrapper，不需要另外安装 Maven。

## 三、克隆项目

```powershell
git clone https://github.com/The-King-A/resume-thinking.git
cd resume-thinking
```

进入的目录应包含 `back`、`front`、`database`、`contracts` 等文件夹。

## 四、创建本地配置

```powershell
Copy-Item .env.example .env
notepad .env
```

至少需要检查并填写以下配置：

```env
MYSQL_USERNAME=你的MySQL用户名
MYSQL_PASSWORD=你的MySQL密码
JWT_SIGNING_KEY_BASE64=新的32字节Base64密钥
APP_ENCRYPTION_KEY_BASE64=新的32字节Base64密钥
PYTHON_INTERNAL_SERVICE_TOKEN=新的至少32位随机字符串
```

其他本地服务默认使用以下地址：

```env
MYSQL_URL=jdbc:mysql://127.0.0.1:3306/resume_thinking
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
PYTHON_ANALYSIS_BASE_URL=http://127.0.0.1:8000
JAVA_CALLBACK_BASE_URL=http://127.0.0.1:8080
MATCHING_CALLBACK_URL=http://127.0.0.1:8080/internal/v2/analysis-results
```

`.env` 仅用于当前电脑，不要提交到 GitHub。

如果只需要全新运行，可以使用新的密钥。如果需要读取旧电脑上的账号、简历和匹配结果，必须恢复旧数据库，并保留旧的 `APP_ENCRYPTION_KEY_BASE64`。

## 五、初始化 MySQL

先创建数据库：

```powershell
mysql -u root -p -e "CREATE DATABASE resume_thinking CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
```

对于全新的空数据库，导入项目结构：

```powershell
Get-Content -Raw database\resume_thinking_schema.sql | mysql -u root -p resume_thinking
```

然后在 `.env` 中临时设置：

```env
SPRING_FLYWAY_BASELINE_ON_MIGRATE=true
SPRING_FLYWAY_BASELINE_VERSION=8
```

Java 首次启动成功后，将这两项清空：

```env
SPRING_FLYWAY_BASELINE_ON_MIGRATE=
SPRING_FLYWAY_BASELINE_VERSION=
```

不要手工创建或修改 `flyway_schema_history`。

如果是恢复旧数据库，不要再次导入新库结构，也不要把新库初始化流程和旧库迁移流程混用；应先备份旧库，再按照当前项目的 Flyway 迁移顺序启动 Java。

## 六、安装项目依赖

安装前端依赖：

```powershell
pnpm --dir front install
```

安装 Python 依赖：

```powershell
py -3.11 -m pip install -e "back/python[test]"
```

## 七、启动 Redis

确保 Docker Desktop 已启动，然后在项目根目录执行：

```powershell
docker compose -f docker-compose.redis.yml up -d
```

停止 Redis：

```powershell
docker compose -f docker-compose.redis.yml down
```

## 八、启动三个服务

请打开三个 PowerShell 窗口，并保持它们运行。

### 窗口一：Python 分析服务

在项目根目录执行：

```powershell
py -3.11 -m uvicorn app.main:app --app-dir back\python --host 127.0.0.1 --port 8000
```

检查：访问 `http://127.0.0.1:8000/health`，应返回：

```json
{"status":"ok"}
```

### 窗口二：Java 公共 API

在项目根目录执行：

```powershell
Get-Content .env |
  Where-Object { $_ -match '^[A-Z0-9_]+=' } |
  ForEach-Object {
    $name, $value = $_ -split '=', 2
    Set-Item -Path "Env:$name" -Value $value
  }

Push-Location back\java
try { .\mvnw.cmd spring-boot:run '-Dspring-boot.run.profiles=local' }
finally { Pop-Location }
```

检查：访问 `http://127.0.0.1:8080/actuator/health`，应显示服务状态为 `UP`。

### 窗口三：Vue 前端

在项目根目录执行：

```powershell
pnpm --dir front dev
```

浏览器访问：

```text
http://127.0.0.1:5173
```

## 九、可选验证

验证前端构建：

```powershell
pnpm --dir front run build
```

验证 Python 测试：

```powershell
py -3.11 -m pytest back/python/tests -q
```

验证跨服务 MVP 流程：

```powershell
powershell -ExecutionPolicy Bypass -File tests/integration/run_mvp_flow.ps1
```

## 十、常见问题

### 页面能打开，但登录或匹配失败

确认 Java、Python、Redis 和 MySQL 都已启动，并检查三个服务端口是否正确。

### 匹配任务一直等待

通常是 Python 分析服务未启动，或者 Java 与 Python 的 `PYTHON_INTERNAL_SERVICE_TOKEN` 不一致。

### 简历解密失败

通常是使用了不同的 `APP_ENCRYPTION_KEY_BASE64`。恢复旧数据库时必须使用旧电脑的加密密钥。

### 前端依赖安装失败

确认 Node.js 和 pnpm 已安装，并在项目根目录执行：

```powershell
pnpm --version
node --version
```

### 修改 `.env` 后配置没有生效

停止并重新启动 Java、Python 服务。已经运行的进程不会自动读取修改后的环境变量。

